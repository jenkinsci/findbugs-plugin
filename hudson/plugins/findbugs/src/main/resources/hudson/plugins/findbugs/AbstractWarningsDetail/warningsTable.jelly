
<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler"
	xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson"
	xmlns:f="/lib/form" xmlns:i="jelly:fmt">

	<h2>Warnings</h2>
	<j:forEach var="warning" items="${it.warnings}">
		<table class="pane" id="summary">
			<tr>
			<td class="pane-header">
				<j:choose>
					<j:when test="${it.isCurrent() &amp;&amp; warning.fileName != null}">
						Class: <a href="${warning.key}/#${warning.lineNumber}">${warning.classname}</a>,
					</j:when>
					<j:otherwise>
						Class: ${warning.classname},
					</j:otherwise>
				</j:choose>
				Type: ${warning.type},
				Priority: ${warning.priority},
				Category: ${warning.category}</td>
			</tr>
		</table>
		<p><b>${warning.message}</b></p>
		<p>${warning.toolTip}</p>
	</j:forEach>

</j:jelly>
