<!--
  Section header

-->
<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core"
         xmlns:f="/lib/form">

    <f:entry title="${%name.includePattern}" field="includePattern" description="${%description.includePattern}">
      <f:textbox />
    </f:entry>
    <f:entry title="${%name.excludePattern}" field="excludePattern" description="${%description.excludePattern}">
      <f:textbox />
    </f:entry>

</j:jelly>